{
  "name": "cag-spawn",
  "version": "1.0.0",
  "private": true,
  "description": "Ctrl-Alt-GG Spawn — the link-collection landing pad people open during the event.",
  "author": "Ctrl-Alt-GG",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev:css": "tailwindcss -i ./assets/css/main.css -o ./assets/css/compiled/main.css --watch",
    "dev:hugo": "hugo server",
    "dev": "npm run build:css && (npm run dev:css & npm run dev:hugo)",
    "build:css": "tailwindcss -i ./assets/css/main.css -o ./assets/css/compiled/main.css",
    "build:hugo": "hugo",
    "build": "npm run build:css && npm run build:hugo"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^4.2.2"
  }
}
